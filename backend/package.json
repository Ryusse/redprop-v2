{
	"name": "Equipo2-tarde-sp7",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"dev": "tsnd --respawn --clear src/app.ts",
		"build": "rimraf ./dist && tsc",
		"start": "npm run build && node dist/app.js",
		"test": "jest",
		"test:watch": "jest --watch",
		"test:coverage": "jest --coverage",
		"db:migrate": "ts-node --transpile-only scripts/migrate-db.ts",
		"db:setup": "npm run db:migrate && npm run db:seed-roles && npm run db:seed-catalogs && npm run db:seed-geography && npm run db:seed-admin",
		"db:drop": "ts-node --transpile-only scripts/drop-db.ts",
		"db:reset": "npm run db:drop && npm run db:migrate && npm run db:seed-roles && npm run db:seed-catalogs && npm run db:seed-geography && npm run db:seed-admin",
		"db:check": "ts-node --transpile-only scripts/check-db.ts",
		"db:seed-roles": "ts-node --transpile-only scripts/seed-roles.ts",
		"db:seed-catalogs": "ts-node --transpile-only scripts/seed-catalogs.ts",
		"db:seed-geography": "ts-node --transpile-only scripts/seed-geography.ts",
		"db:seed-admin": "ts-node --transpile-only scripts/seed-admin.ts",
		"cleanup:tokens": "ts-node --transpile-only scripts/cleanup-revoked-tokens.ts"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/bcrypt": "^6.0.0",
		"@types/express": "^4.17.18",
		"@types/jsonwebtoken": "^9.0.10",
		"@types/multer": "^2.0.0",
		"@types/node": "^20.8.0",
		"@types/pg": "^8.15.6",
		"@types/swagger-jsdoc": "^6.0.4",
		"@types/swagger-ui-express": "^4.1.8",
		"@types/uuid": "^10.0.0",
		"rimraf": "^5.0.5",
		"ts-node": "^10.9.2",
		"ts-node-dev": "^2.0.0",
		"typescript": "^5.2.2"
	},
	"dependencies": {
		"bcrypt": "^6.0.0",
		"cloudinary": "^2.8.0",
		"dotenv": "^16.3.1",
		"env-var": "^7.4.1",
		"express": "^4.18.2",
		"jsonwebtoken": "^9.0.2",
		"multer": "^2.0.2",
		"pg": "^8.16.3",
		"swagger-jsdoc": "^6.2.8",
		"swagger-ui-express": "^5.0.1",
		"uuid": "^13.0.0"
	}
}
